Dawson Landscaping — responsive HTML/CSS recreation
Open index.html in a browser. No build step is required.
Assets are cropped directly from the supplied screenshot so the visible imagery/logo remains consistent with the reference.
